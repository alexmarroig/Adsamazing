{
  "name": "@ads/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "pnpm --filter @ads/db build && npm run prisma:generate && tsup src/index.ts --format esm,cjs --dts --no-external @ads/db",
    "build": "pnpm --filter @ads/db build && npm run prisma:generate && tsup src/index.ts --format esm,cjs --dts",
    "build": "pnpm --filter @ads/db build && pnpm run prisma:generate && tsup src/index.ts --format esm,cjs --dts",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prisma:generate": "prisma generate --schema ../../packages/db/prisma/schema.prisma"
  },
  "dependencies": {
    "@ads/db": "file:../../packages/db",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prisma": "^6.1.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
