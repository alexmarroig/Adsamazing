{
  "name": "google-oauth-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @ads/web --filter @ads/api --filter @ads/worker dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @ads/web start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "build:api": "pnpm --filter @ads/api... build",
    "start:api": "pnpm --filter @ads/api start"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1",
    "typescript": "5.7.2"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma",
      "sharp",
      "msgpackr-extract",
      "unrs-resolver"
    ]
  }
}

