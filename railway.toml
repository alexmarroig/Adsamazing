[build]
builder = "NIXPACKS"
buildCommand = "corepack enable && pnpm install && pnpm --filter @ads/api... build"

[deploy]
startCommand = "pnpm --filter @ads/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
